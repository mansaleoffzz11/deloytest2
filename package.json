{
  "name": "meta-verification",
  "version": "1.0.0",
  "description": "Meta Verification System",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "node dev-server.mjs",
    "vercel-build": "node build.js --obfuscate",
    "build": "node build.js --obfuscate",
    "build:minify": "node build.js --minify",
    "build:obfuscate": "node build.js --obfuscate",
    "clean": "rimraf dist",
    "build:css": "tailwindcss -i ./src/site.css -o ./public/styles/tw-built.css --minify"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "esbuild": "^0.28.0",
    "fs-extra": "^11.2.0",
    "intl-tel-input": "^27.0.0",
    "javascript-obfuscator": "^4.1.0",
    "rimraf": "^5.0.5",
    "tailwindcss": "^3.4.19",
    "terser": "^5.26.0",
    "tick-xanh-workspace": "file:.."
  }
}
